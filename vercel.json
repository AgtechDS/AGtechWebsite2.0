{
  "framework": "vite",
  "buildCommand": "npm install --legacy-peer-deps && npm run dev",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
